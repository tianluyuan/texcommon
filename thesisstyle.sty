\ProvidesPackage{texcommon/thesisstyle}
% 'hyperref' breaks when compiled with CU thesis.cls so use 'url' instead
% \usepackage{hyperref}
\usepackage{url}
% use natbib for sorting citation numbers
\usepackage[numbers,sort]{natbib}
% allows formatting titles in appendices
% cleveref should be loaded after all other packages that modify
% referencing system
\usepackage[capitalize]{cleveref}
